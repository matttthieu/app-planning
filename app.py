# app.pyimport streamlit as stimport pandas as pdimport datetimedef generate_response(review_text, rating):    if rating >= 4:        return f"Merci beaucoup pour votre avis positif ! Nous sommes ravis que vous ayez apprécié votre expérience. Au plaisir de vous revoir bientôt !"    elif rating == 3:        return f"Merci pour votre retour. Nous prenons en compte vos commentaires pour nous améliorer. N'hésitez pas à nous faire part de vos suggestions lors de votre prochaine visite."    else:        return f"Nous vous présentons nos sincères excuses pour cette expérience qui ne correspond pas à nos standards. Pouvez-vous nous contacter directement pour que nous puissions rectifier la situation?"def main():    st.title("🤖 Assistant Réponse Avis Clients")        # Interface principale    st.write("Bienvenue dans votre assistant de réponse aux avis clients")        # Zone de saisie    review = st.text_area("Entrez l'avis du client ici :", height=100)    rating = st.slider("Note du client :", 1, 5, 5)    platform = st.selectbox("Plateforme :", ["Google", "TripAdvisor"])        if st.button("Générer une réponse"):        if review:            response = generate_response(review, rating)            st.text_area("Réponse générée :", response, height=150)                        # Sauvegarder l'historique            if 'history' not in st.session_state:                st.session_state.history = []                        st.session_state.history.append({                'date': datetime.datetime.now().strftime("%Y-%m-%d %H:%M"),                'platform': platform,                'rating': rating,                'review': review,                'response': response            })        # Afficher l'historique    if st.session_state.get('history'):        st.subheader("Historique des réponses")        df = pd.DataFrame(st.session_state.history)        st.dataframe(df)                # Bouton d'export        if st.button("Exporter l'historique (CSV)"):            csv = df.to_csv(index=False)            st.download_button(                "📥 Télécharger CSV",                csv,                "historique_reponses.csv",                "text/csv"            )if __name__ == "__main__":    main()